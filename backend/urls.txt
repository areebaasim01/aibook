# Example URLs for the Book Embeddings Ingestion Pipeline
# Add one URL per line

# You can now use the --sitemap argument to load all URLs from sitemap.xml
# Example: python -m backend.src.main --sitemap https://aibook-wrcb.vercel.app/docs/robotic-nervous-system/sitemap.xml

# Or use the default file approach by uncommenting the line below:
# https://aibook-wrcb.vercel.app/

# Add more URLs as needed for your documentation